
{%- style -%}
  .sec-store {
    display: flex;
    align-items: center;
    width: 100%;
    min-height: 72.46vw;
    margin: 0;
    background: url({{ "index_store-bg_sp.jpg" | asset_url }}) center center no-repeat;
    background-size: cover;
  }
  .section-{{ section.id }}-padding {
    width: 100%;
    padding-top: 1rem;
    padding-bottom: 2.5rem;
  }
  html[fontify-lang="ja"] h2.h2-store-title {
    margin: 0;
    font-family: 'Optima' !important;
    text-align: center;
    font-size: 56px;
    line-height: 1.3;
    color: #fff;
  }
  .txt-store-summary {
    margin: 10px 0 0;
    color: #fff;
    line-height: 1.8;
  }
  .btn-store-top {
    text-align: center;
  }
  .section-{{ section.id }}-padding .button {
    margin-top: 50px;
    background: transparent;
    color: #fff;
  }
  .section-{{ section.id }}-padding .button::after {
    box-shadow: 0 0 0 2px rgba(255,255,255,1.0);  
  }
  .section-{{ section.id }}-padding .button:hover::after {
    box-shadow: 0 0 0 3px rgba(255,255,255,1.0);  
  }
  
  @media screen and (min-width: 750px) {
    .sec-store {
      height: 370px;
      min-height: auto;
      background-image: url({{ "index_store-bg_pc.jpg" | asset_url }});
    }
    html[fontify-lang="ja"] h2.h2-store-title {
      text-align: left;
      font-size: 70px;
    }
    .txt-store-summary {
      margin: 15px 0 0;
    }
    .txt-store-summary span {
      display: inline-block;
      max-width: 55rem;
    }
    .btn-store-top {
      text-align: left;
    }
    .section-{{ section.id }}-padding .button {
      margin-top: 60px;
    }
  }
  @media screen and (min-width: 1360px) {
    .sec-store {
      height: 27.09vw;
    }
  }
{%- endstyle -%}


<div class="sec-store">
  <div class="page-width section-{{ section.id }}-padding">
    <h2 class="h2-store-title">STORE</h2>
    <p class="txt-store-summary"><span>いまWSYで扱っている木材は、主にウエスタン・レッドシダーという、ヒノキの仲間。
      腐りにくいので、北米では屋根や外壁にも広く使われていて、風合いが良く軽いのが特徴です。
      流通の過程で規格外になり、使われないままストックヤードに積まれてきた、貴重な木材を活用しよう！</span></p>
    <div class="btn-store-top"><a href="/collections/all" class="button">ストアーへ</a></div>
  </div>
</div>




{% schema %}
  {
    "name": "index-store",
    "settings": [
    ]
  }
{% endschema %}
